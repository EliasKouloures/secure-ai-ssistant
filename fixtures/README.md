# Synthetic Fixtures

This folder ships with synthetic, anonymised regression fixtures only.

Fixture groups:

- `text/` pasted email-style examples
- `notes/` typed call-note examples
- `expected/` expected extracted fields for regression checks
- generated PDFs and images will sit alongside the matching expected payloads

No real school data is included.

